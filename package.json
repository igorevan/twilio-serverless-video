{
  "name": "serverless-video-webrtc",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run",
    "deploy": "twilio-run deploy"
  },
  "dependencies": {
    "@twilio/runtime-handler": "1.2.2",
    "dotenv": "^16.1.4",
    "socket.io": "^4.6.1",
    "socket.io-client": "^4.6.1",
    "twilio": "^3.56",
    "ws": "^8.13.0"
  },
  "devDependencies": {
    "twilio-run": "^3.4.1"
  },
  "engines": {
    "node": "16"
  }
}
